<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="rodrigo.zaniolo.myshowcaseapp.utils.FontUtils.FontName"/>
        <variable
            name="presenter"
            type="rodrigo.zaniolo.myshowcaseapp.main.MainInterface.Presenter"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_gradient_primary">

    <LinearLayout
        android:id="@+id/myLinearLayoutMainActivityLists"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="8dp"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginLeft="8dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="8dp">

        <RelativeLayout
            android:id="@+id/myRelativeLayoutAccountDatenext"
            style="@style/AppTheme.RelativeLayout.App.Button.Primary"
            android:gravity="center"
            android:clickable="true"
            android:onClick="@{presenter.onWeather()}">

            <TextView
                android:id="@+id/myTextViewIncomeInformationNext"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:visibility="visible"
                android:text="@string/main_weather_button"
                android:textSize="18sp"
                android:textColor="@color/white"
                app:font="@{FontName.BOLD}"/>
        </RelativeLayout>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>

</layout>